# Environment variables
.env

# Docker volumes/data
db-data/
pgadmin-data/
# If your volumes were named volumes rather than bind mounts
# and were created by docker-compose, you might also ignore:
# *.sql # If you generate temporary SQL dumps that you don't want versioned

# Python virtual environment
venv/

# Python compilation artifacts and temporary files
__pycache__/
*.pyc
*.log
.pytest_cache/
.mypy_cache/
.envrc
*.sqlite3 # If you were using SQLite databases

notes.txt
django_helpdesk/
